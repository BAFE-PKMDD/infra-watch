Place the custom openWakeWord ONNX model here as `hey_ania.onnx`.

Train/export it with the official openWakeWord Colab workflow using positive examples for “Hey ANIA”. Do not rename a stock model: a wake phrase requires a model trained for that phrase.
